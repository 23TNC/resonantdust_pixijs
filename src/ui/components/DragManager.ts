// DragManager has been split into:
//   • ui/components/DragOverlay.ts        — the visual overlay (cursor lerp,
//                                            return tween, gap shrink, entry pool)
//   • coordinators/DragController.ts      — input handling and drop semantics
//
// This file is intentionally empty.  Delete it from the repo when convenient.
export {};
